version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: binarygate-website:latest
    environment:
      NODE_ENV: production
    ports:
      - "3000:3000"
